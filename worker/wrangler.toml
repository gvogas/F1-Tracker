name = "f1-ai-proxy"
main = "index.js"
compatibility_date = "2024-09-23"

# Secrets (set via CLI — never put real values here):
#   wrangler secret put HF_TOKEN
#
# Local dev secrets go in .dev.vars (gitignored):
#   echo 'HF_TOKEN=hf_xxxxxxx' > .dev.vars
#   wrangler dev
